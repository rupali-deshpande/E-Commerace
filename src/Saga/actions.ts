import {productsActions} from '../store/productslice'
import { ProdAddNew } from '../types';
// import { newproductsagaAction } from './sagaaction';

export const updateFormFields = (data: ProdAddNew) => ({
    // type: newproductsagaAction.NEWLY_ADD_DATA,
    // data
  });
  

